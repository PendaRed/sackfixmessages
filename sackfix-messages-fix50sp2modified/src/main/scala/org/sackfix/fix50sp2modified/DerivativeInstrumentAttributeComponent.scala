package org.sackfix.fix50sp2modified

import org.sackfix.field._
import org.sackfix.common.validated.fields.{SfFixComponent, SfFixDecoder, SfFixFieldsToAscii, SfFixRenderable}
import org.sackfix.common.message.SfRepeatingGroupCountException
import scala.annotation.tailrec
import scala.collection.immutable.HashSet
import scala.collection.mutable.ArrayBuffer


/**
  * Generated by SackFix code generator on 20170404
  * Source specification was read from:
  *   /quickfixj1.6.0/FIX50SP2.modified.xml
  */
case class DerivativeInstrumentAttributeComponent(noDerivativeInstrAttribField:Option[NoDerivativeInstrAttribField]=None,
                                                  derivativeInstrAttribGroups: Option[List[DerivativeInstrAttribGroup]]=None) extends SfFixComponent  with SfFixRenderable with SfFixFieldsToAscii {
  if (noDerivativeInstrAttribField.map(_.value).getOrElse(0) != derivativeInstrAttribGroups.map(_.size).getOrElse(0))
    throw SfRepeatingGroupCountException(NoDerivativeInstrAttribField.TagId,noDerivativeInstrAttribField.map(_.value).getOrElse(0), derivativeInstrAttribGroups.map(_.size).getOrElse(0))

  override lazy val fixStr : String = appendFixStr().toString
  override def appendFixStr(b:StringBuilder = new StringBuilder): StringBuilder = format(formatForFix, b)

  override def toString():String = appendStringBuilder().toString()
  def appendStringBuilder(b:StringBuilder = new StringBuilder) : StringBuilder = format(formatForToString, b)

  def format( fmt: ((StringBuilder,SfFixRenderable)=>Unit), b:StringBuilder = new StringBuilder()): StringBuilder = {
    noDerivativeInstrAttribField.foreach(fmt(b,_))
    derivativeInstrAttribGroups.getOrElse(List.empty).foreach(fmt(b,_))
    b
  }

}
     
object DerivativeInstrumentAttributeComponent extends SfFixDecoder {

  override val MandatoryFields = HashSet[Int]()

  override def isMandatoryField(tagId:Int) : Boolean = {
    MandatoryFields.contains(tagId)  || 
    DerivativeInstrAttribGroup.isMandatoryField(tagId)
  }

  override val OptionalFields = HashSet[Int](
    NoDerivativeInstrAttribField.TagId)

  override def isOptionalField(tagId:Int) : Boolean = {
    OptionalFields.contains(tagId)  || 
    DerivativeInstrAttribGroup.isOptionalField(tagId)
  }

  override def isFieldOf(tagId:Int) : Boolean = isMandatoryField(tagId) || isOptionalField(tagId)  || 
    DerivativeInstrAttribGroup.isFieldOf(tagId)

   override lazy val RepeatingGroupsTags = HashSet[Int](
    NoDerivativeInstrAttribField.TagId)
  
      
  override def isFirstField(tagId:Int) : Boolean = DerivativeInstrAttribGroup.isFirstField(tagId) 

  def decode(flds: Seq[Tuple2[Int, Any]], startPos:Int = 0):Option[DerivativeInstrumentAttributeComponent] = {
    val (pos, myFields, nextTagPosLookup) = extractMyFieldsAndPopulatePositions(false, flds, startPos)
    validateMandatoryFieldsPresent(myFields)

    if (myFields.nonEmpty) {
      Some(DerivativeInstrumentAttributeComponent(myFields.get(NoDerivativeInstrAttribField.TagId).flatMap(f=>NoDerivativeInstrAttribField.decode(f)),
        if (nextTagPosLookup.contains(NoDerivativeInstrAttribField.TagId)) DerivativeInstrAttribGroup.decode(flds, nextTagPosLookup(NoDerivativeInstrAttribField.TagId)) else None))
    } else None
  }

    
}
     