package org.sackfix.fix50sp2modified

import org.sackfix.field._
import org.sackfix.common.validated.fields.{SfFixComponent, SfFixDecoder, SfFixFieldsToAscii, SfFixRenderable}
import org.sackfix.common.message.SfRepeatingGroupCountException
import scala.annotation.tailrec
import scala.collection.immutable.HashSet
import scala.collection.mutable.ArrayBuffer


/**
  * Generated by SackFix code generator on 20170404
  * Source specification was read from:
  *   /quickfixj1.6.0/FIX50SP2.modified.xml
  */
case class UndlyInstrumentPartiesComponent(noUndlyInstrumentPartiesField:Option[NoUndlyInstrumentPartiesField]=None,
                                           undlyInstrumentPartiesGroups: Option[List[UndlyInstrumentPartiesGroup]]=None) extends SfFixComponent  with SfFixRenderable with SfFixFieldsToAscii {
  if (noUndlyInstrumentPartiesField.map(_.value).getOrElse(0) != undlyInstrumentPartiesGroups.map(_.size).getOrElse(0))
    throw SfRepeatingGroupCountException(NoUndlyInstrumentPartiesField.TagId,noUndlyInstrumentPartiesField.map(_.value).getOrElse(0), undlyInstrumentPartiesGroups.map(_.size).getOrElse(0))

  override lazy val fixStr : String = appendFixStr().toString
  override def appendFixStr(b:StringBuilder = new StringBuilder): StringBuilder = format(formatForFix, b)

  override def toString():String = appendStringBuilder().toString()
  def appendStringBuilder(b:StringBuilder = new StringBuilder) : StringBuilder = format(formatForToString, b)

  def format( fmt: ((StringBuilder,SfFixRenderable)=>Unit), b:StringBuilder = new StringBuilder()): StringBuilder = {
    noUndlyInstrumentPartiesField.foreach(fmt(b,_))
    undlyInstrumentPartiesGroups.getOrElse(List.empty).foreach(fmt(b,_))
    b
  }

}
     
object UndlyInstrumentPartiesComponent extends SfFixDecoder {

  override val MandatoryFields = HashSet[Int]()

  override def isMandatoryField(tagId:Int) : Boolean = {
    MandatoryFields.contains(tagId)  || 
    UndlyInstrumentPartiesGroup.isMandatoryField(tagId)
  }

  override val OptionalFields = HashSet[Int](
    NoUndlyInstrumentPartiesField.TagId)

  override def isOptionalField(tagId:Int) : Boolean = {
    OptionalFields.contains(tagId)  || 
    UndlyInstrumentPartiesGroup.isOptionalField(tagId)
  }

  override def isFieldOf(tagId:Int) : Boolean = isMandatoryField(tagId) || isOptionalField(tagId)  || 
    UndlyInstrumentPartiesGroup.isFieldOf(tagId)

   override lazy val RepeatingGroupsTags = HashSet[Int](
    NoUndlyInstrumentPartiesField.TagId)
  
      
  override def isFirstField(tagId:Int) : Boolean = UndlyInstrumentPartiesGroup.isFirstField(tagId) 

  def decode(flds: Seq[Tuple2[Int, Any]], startPos:Int = 0):Option[UndlyInstrumentPartiesComponent] = {
    val (pos, myFields, nextTagPosLookup) = extractMyFieldsAndPopulatePositions(false, flds, startPos)
    validateMandatoryFieldsPresent(myFields)

    if (myFields.nonEmpty) {
      Some(UndlyInstrumentPartiesComponent(myFields.get(NoUndlyInstrumentPartiesField.TagId).flatMap(f=>NoUndlyInstrumentPartiesField.decode(f)),
        if (nextTagPosLookup.contains(NoUndlyInstrumentPartiesField.TagId)) UndlyInstrumentPartiesGroup.decode(flds, nextTagPosLookup(NoUndlyInstrumentPartiesField.TagId)) else None))
    } else None
  }

    
}
     