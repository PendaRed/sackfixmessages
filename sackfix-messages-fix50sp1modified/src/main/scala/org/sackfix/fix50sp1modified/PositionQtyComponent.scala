package org.sackfix.fix50sp1modified

import org.sackfix.field._
import org.sackfix.common.validated.fields.{SfFixComponent, SfFixDecoder, SfFixFieldsToAscii, SfFixRenderable}
import org.sackfix.common.message.SfRepeatingGroupCountException
import scala.annotation.tailrec
import scala.collection.immutable.HashSet
import scala.collection.mutable.ArrayBuffer


/**
  * Generated by SackFix code generator on 20210314
  * Source specification was read from:
  *   /quickfixj1.6.0/FIX50SP1.modified.xml
  */
case class PositionQtyComponent(noPositionsField:Option[NoPositionsField]=None,
                                positionsGroups: Option[List[PositionsGroup]]=None) extends SfFixComponent  with SfFixRenderable with SfFixFieldsToAscii {
  if (noPositionsField.map(_.value).getOrElse(0) != positionsGroups.map(_.size).getOrElse(0))
    throw SfRepeatingGroupCountException(NoPositionsField.TagId,noPositionsField.map(_.value).getOrElse(0), positionsGroups.map(_.size).getOrElse(0))

  override lazy val fixStr : String = appendFixStr().toString
  override def appendFixStr(b:StringBuilder = new StringBuilder): StringBuilder = format(formatForFix, b)

  override def toString():String = appendStringBuilder().toString()
  def appendStringBuilder(b:StringBuilder = new StringBuilder) : StringBuilder = format(formatForToString, b)

  def format( fmt: ((StringBuilder,SfFixRenderable)=>Unit), b:StringBuilder = new StringBuilder()): StringBuilder = {
    noPositionsField.foreach(fmt(b,_))
    positionsGroups.getOrElse(List.empty).foreach(fmt(b,_))
    b
  }

}
     
object PositionQtyComponent extends SfFixDecoder {

  override val MandatoryFields = HashSet[Int]()

  override def isMandatoryField(tagId:Int) : Boolean = {
    MandatoryFields.contains(tagId)  || 
    PositionsGroup.isMandatoryField(tagId)
  }

  override val OptionalFields = HashSet[Int](
    NoPositionsField.TagId)

  override def isOptionalField(tagId:Int) : Boolean = {
    OptionalFields.contains(tagId)  || 
    PositionsGroup.isOptionalField(tagId)
  }

  override def isFieldOf(tagId:Int) : Boolean = isMandatoryField(tagId) || isOptionalField(tagId)  || 
    PositionsGroup.isFieldOf(tagId)

   override lazy val RepeatingGroupsTags = HashSet[Int](
    NoPositionsField.TagId)
  
      
  override def isFirstField(tagId:Int) : Boolean = PositionsGroup.isFirstField(tagId) 

  def decode(flds: Seq[Tuple2[Int, Any]], startPos:Int = 0):Option[PositionQtyComponent] = {
    val (pos, myFields, nextTagPosLookup) = extractMyFieldsAndPopulatePositions(false, flds, startPos)
    validateMandatoryFieldsPresent(myFields)

    if (myFields.nonEmpty) {
      Some(PositionQtyComponent(myFields.get(NoPositionsField.TagId).flatMap(f=>NoPositionsField.decode(f)),
        if (nextTagPosLookup.contains(NoPositionsField.TagId)) PositionsGroup.decode(flds, nextTagPosLookup(NoPositionsField.TagId)) else None))
    } else None
  }

    
}
     