package org.sackfix.fix50sp1

import org.sackfix.field._
import org.sackfix.common.validated.fields.{SfFixComponent, SfFixDecoder, SfFixFieldsToAscii, SfFixRenderable}
import org.sackfix.common.message.SfRepeatingGroupCountException
import scala.annotation.tailrec
import scala.collection.immutable.HashSet
import scala.collection.mutable.ArrayBuffer


/**
  * Generated by SackFix code generator on 20170404
  * Source specification was read from:
  *   /quickfixj1.6.0/FIX50SP1.xml
  */
case class CollInqQualGrpComponent(noCollInquiryQualifierField:Option[NoCollInquiryQualifierField]=None,
                                   collInquiryQualifierGroups: Option[List[CollInquiryQualifierGroup]]=None) extends SfFixComponent  with SfFixRenderable with SfFixFieldsToAscii {
  if (noCollInquiryQualifierField.map(_.value).getOrElse(0) != collInquiryQualifierGroups.map(_.size).getOrElse(0))
    throw SfRepeatingGroupCountException(NoCollInquiryQualifierField.TagId,noCollInquiryQualifierField.map(_.value).getOrElse(0), collInquiryQualifierGroups.map(_.size).getOrElse(0))

  override lazy val fixStr : String = appendFixStr().toString
  override def appendFixStr(b:StringBuilder = new StringBuilder): StringBuilder = format(formatForFix, b)

  override def toString():String = appendStringBuilder().toString()
  def appendStringBuilder(b:StringBuilder = new StringBuilder) : StringBuilder = format(formatForToString, b)

  def format( fmt: ((StringBuilder,SfFixRenderable)=>Unit), b:StringBuilder = new StringBuilder()): StringBuilder = {
    noCollInquiryQualifierField.foreach(fmt(b,_))
    collInquiryQualifierGroups.getOrElse(List.empty).foreach(fmt(b,_))
    b
  }

}
     
object CollInqQualGrpComponent extends SfFixDecoder {

  override val MandatoryFields = HashSet[Int]()

  override def isMandatoryField(tagId:Int) : Boolean = {
    MandatoryFields.contains(tagId)  || 
    CollInquiryQualifierGroup.isMandatoryField(tagId)
  }

  override val OptionalFields = HashSet[Int](
    NoCollInquiryQualifierField.TagId)

  override def isOptionalField(tagId:Int) : Boolean = {
    OptionalFields.contains(tagId)  || 
    CollInquiryQualifierGroup.isOptionalField(tagId)
  }

  override def isFieldOf(tagId:Int) : Boolean = isMandatoryField(tagId) || isOptionalField(tagId)  || 
    CollInquiryQualifierGroup.isFieldOf(tagId)

   override lazy val RepeatingGroupsTags = HashSet[Int](
    NoCollInquiryQualifierField.TagId)
  
      
  override def isFirstField(tagId:Int) : Boolean = CollInquiryQualifierGroup.isFirstField(tagId) 

  def decode(flds: Seq[Tuple2[Int, Any]], startPos:Int = 0):Option[CollInqQualGrpComponent] = {
    val (pos, myFields, nextTagPosLookup) = extractMyFieldsAndPopulatePositions(false, flds, startPos)
    validateMandatoryFieldsPresent(myFields)

    if (myFields.nonEmpty) {
      Some(CollInqQualGrpComponent(myFields.get(NoCollInquiryQualifierField.TagId).flatMap(f=>NoCollInquiryQualifierField.decode(f)),
        if (nextTagPosLookup.contains(NoCollInquiryQualifierField.TagId)) CollInquiryQualifierGroup.decode(flds, nextTagPosLookup(NoCollInquiryQualifierField.TagId)) else None))
    } else None
  }

    
}
     